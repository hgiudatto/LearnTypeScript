const superHeroes: string[] = [];
const heroePower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

const MLModels: number[][] = [[255, 255, 255], []];

superHeroes.push("Batman");
heroePower.push(5);

allUsers.push({ name: "", isActive: false });
